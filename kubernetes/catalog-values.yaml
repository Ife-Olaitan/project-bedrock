# Catalog service - uses external RDS MySQL
mysql:
  create: false # No in-cluster MySQL

app:
  persistence:
    provider: mysql
    # endpoint is set via --set in deploy script (needs Terraform output)
    database: catalog_db
    secret:
      create: false  # Use ESO-managed secret
      name: catalog-db-credentials